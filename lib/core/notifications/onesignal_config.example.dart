/// Template for `onesignal_config.dart` (gitignored — see README).
/// Copy this file to `onesignal_config.dart` and fill in your own values
/// from the OneSignal dashboard: Settings > Keys & IDs.
class OneSignalConfig {
  static const appId = 'YOUR_ONESIGNAL_APP_ID';
  static const restApiKey = 'YOUR_ONESIGNAL_REST_API_KEY';
}
